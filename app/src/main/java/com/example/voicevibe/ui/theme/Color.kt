package com.example.voicevibe.ui.theme

import androidx.compose.ui.graphics.Color

// Neutral clean palette
val NeutralWhite = Color(0xFFFFFFFF)   // Pure white
val NeutralLightGray = Color(0xFFF5F5F5) // Soft light gray
val NeutralGray = Color(0xFFE0E0E0)    // Medium light gray
val NeutralDarkGray = Color(0xFF424242) // Dark gray for text
val NeutralBlack = Color(0xFF000000)   // Strong black for emphasis

// Accent colors (minimal but for highlights)
val AccentBlueGray = Color(0xFF90A4AE)  // Muted blue-gray accent
val AccentWarmBeige = Color(0xFFFBE9E7) // Subtle warm beige highlight

// Brand palette (logo-inspired)
val BrandNavyDark = Color(0xFF0A1633)   // Deep navy background
val BrandNavy = Color(0xFF0E1D3F)       // Surface navy
val BrandCyan = Color(0xFF2EE6FF)       // Left gradient (cyan/teal)
val BrandIndigo = Color(0xFF4F46E5)     // Middle gradient (indigo)
val BrandFuchsia = Color(0xFFC026D3)    // Right gradient (fuchsia)
